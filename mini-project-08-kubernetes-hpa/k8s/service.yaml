apiVersion: v1
kind: Service
metadata:
  name: hpa-demo-service
  namespace: hpa-demo
spec:
  type: NodePort
  selector:
    app: hpa-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30088
